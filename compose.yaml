services:
  dev:
    build:
      context: ./
      dockerfile: ./dev.Dockerfile
    volumes:
        - ./:/workspace
        - lingma:/root/.lingma
        - ./scripts/entrypoint.sh:/entrypoint
        - docker:/var/lib/docker
        - cache:/root/.cache
    stdin_open: true
    tty: true
    privileged: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
volumes:
  lingma:
  docker:
  cache: